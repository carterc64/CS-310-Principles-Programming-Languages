Carter Copen
This assignment follows academic integrity.


pivot(Before, After) :-
    length(Before, N),
    Half is N - N // 2,
    length(H1, Half),
    append(H1, H2, Before),
    append(H2, H1, After).





    